# Skill: Viz Skill (5-Stage)

## 核心逻辑

1. **数据理解**: 识别字段类型及缺失值。
2. **图表选择**: 映射指令至图表配置，含保底逻辑。
3. **样式生成**: 生成 Plotly 对象，内置 try-except。
4. **可解释输出**: 提取数据洞察；若环节3失败则输出修复建议。
5. **多轮迭代**: 增量修改配置，支持版本控制。

## 异常处理

- **空数据**: 返回错误提示。
- **字段缺失**: 自动匹配数值/类别列。
- **运行报错**: 返回带错误信息的 `go.Figure` 批注。
